services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    container_name: trader-risk-radar-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: trader_risk_radar
    ports:
      - '5432:5432'
    volumes:
      # bind mount 到專案內 ./data/postgres（已被 .gitignore 忽略）
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d trader_risk_radar']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: trader-risk-radar-redis
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10

  # 一次性服務：DB 起來後套用 migration，api / worker 等它成功才啟動。
  migrate:
    build:
      context: .
      target: runtime
    image: trader-risk-radar
    container_name: trader-risk-radar-migrate
    command: ['bunx', 'prisma', 'migrate', 'deploy']
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/trader_risk_radar?schema=public
    depends_on:
      postgres:
        condition: service_healthy
    restart: 'no'

  # REST API（main.ts）。
  api:
    build:
      context: .
      target: runtime
    image: trader-risk-radar
    container_name: trader-risk-radar-api
    command: ['bun', 'run', 'start']
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/trader_risk_radar?schema=public
      REDIS_URL: redis://redis:6379
      PORT: '3000'
    ports:
      - '3000:3000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  # 背景排程 worker（worker.ts）：sync → poll → recompute。
  worker:
    build:
      context: .
      target: runtime
    image: trader-risk-radar
    container_name: trader-risk-radar-worker
    command: ['bun', 'run', 'worker']
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/trader_risk_radar?schema=public
      REDIS_URL: redis://redis:6379
      HYPERLIQUID_API_BASE_URL: https://api.hyperliquid.xyz
      HYPERLIQUID_STATS_DATA_BASE_URL: https://stats-data.hyperliquid.xyz
      MAXIMUM_TRADERS: '200'
      SYNC_INTERVAL_MS: '3600000'
      POLL_INTERVAL_MS: '30000'
      RECOMPUTE_INTERVAL_MS: '300000'
      POLL_LOOKBACK_MS: '7776000000'
      # Hyperliquid /info 限流（per-IP weight 預算）
      REQUEST_WEIGHT_BUDGET: '1200'
      REQUEST_WEIGHT_INTERVAL_MS: '60000'
      # 429 退避（Retry-After 優先，否則 exponential backoff + jitter）
      BACKOFF_MAXIMUM_RETRY_COUNT: '5'
      BACKOFF_BASE_DELAY_MS: '500'
      BACKOFF_MAXIMUM_DELAY_MS: '30000'
      # OKX copytrading 公開 API（免金鑰）+ 限流
      OKX_API_BASE_URL: https://www.okx.com
      OKX_REQUEST_BUDGET: '10'
      OKX_REQUEST_INTERVAL_MS: '2000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  # postgres 改用 bind mount（./data/postgres），不再用具名 volume
  redis-data:
